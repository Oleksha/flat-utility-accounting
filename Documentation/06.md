## Контроллеры для базового CRUD

Оформим контроллеры.

Отлично! Давай оформим **контроллеры для базового CRUD** для квартир, поставщиков и услуг. Я дам **полные контроллеры с методами index, create, store, show, edit, update, destroy**, чтобы они сразу работали с шаблонами, которые мы сделали.

---

## **1. ApartmentController**

```php
<?php

namespace App\Http\Controllers;

use App\Models\Apartment;
use Illuminate\Http\Request;

class ApartmentController extends Controller
{
    public function index()
    {
        $apartments = Apartment::all();
        return view('apartments.index', compact('apartments'));
    }

    public function create()
    {
        return view('apartments.create');
    }

    public function store(Request $request)
    {
        $request->validate([
            'name' => 'required|string|max:255',
            'address' => 'nullable|string|max:255',
        ]);

        Apartment::create($request->all());

        return redirect()->route('apartments.index')->with('success', 'Квартира добавлена');
    }

    public function show(Apartment $apartment)
    {
        return view('apartments.show', compact('apartment'));
    }

    public function edit(Apartment $apartment)
    {
        return view('apartments.edit', compact('apartment'));
    }

    public function update(Request $request, Apartment $apartment)
    {
        $request->validate([
            'name' => 'required|string|max:255',
            'address' => 'nullable|string|max:255',
        ]);

        $apartment->update($request->all());

        return redirect()->route('apartments.index')->with('success', 'Квартира обновлена');
    }

    public function destroy(Apartment $apartment)
    {
        $apartment->delete();
        return redirect()->route('apartments.index')->with('success', 'Квартира удалена');
    }
}
```

---

## **2. ProviderController**

```php
<?php

namespace App\Http\Controllers;

use App\Models\Provider;
use Illuminate\Http\Request;

class ProviderController extends Controller
{
    public function index()
    {
        $providers = Provider::all();
        return view('providers.index', compact('providers'));
    }

    public function create()
    {
        return view('providers.create');
    }

    public function store(Request $request)
    {
        $request->validate([
            'name' => 'required|string|max:255',
            'phone' => 'nullable|string|max:50',
            'email' => 'nullable|email|max:255',
            'site_url' => 'nullable|url|max:255',
        ]);

        Provider::create($request->all());

        return redirect()->route('providers.index')->with('success', 'Поставщик добавлен');
    }

    public function show(Provider $provider)
    {
        return view('providers.show', compact('provider'));
    }

    public function edit(Provider $provider)
    {
        return view('providers.edit', compact('provider'));
    }

    public function update(Request $request, Provider $provider)
    {
        $request->validate([
            'name' => 'required|string|max:255',
            'phone' => 'nullable|string|max:50',
            'email' => 'nullable|email|max:255',
            'site_url' => 'nullable|url|max:255',
        ]);

        $provider->update($request->all());

        return redirect()->route('providers.index')->with('success', 'Поставщик обновлен');
    }

    public function destroy(Provider $provider)
    {
        $provider->delete();
        return redirect()->route('providers.index')->with('success', 'Поставщик удален');
    }
}
```

---

## **3. ServiceController**

```php
<?php

namespace App\Http\Controllers;

use App\Models\Service;
use Illuminate\Http\Request;

class ServiceController extends Controller
{
    public function index()
    {
        $services = Service::all();
        return view('services.index', compact('services'));
    }

    public function create()
    {
        return view('services.create');
    }

    public function store(Request $request)
    {
        $request->validate([
            'name' => 'required|string|max:255',
        ]);

        Service::create($request->all());

        return redirect()->route('services.index')->with('success', 'Услуга добавлена');
    }

    public function show(Service $service)
    {
        return view('services.show', compact('service'));
    }

    public function edit(Service $service)
    {
        return view('services.edit', compact('service'));
    }

    public function update(Request $request, Service $service)
    {
        $request->validate([
            'name' => 'required|string|max:255',
        ]);

        $service->update($request->all());

        return redirect()->route('services.index')->with('success', 'Услуга обновлена');
    }

    public function destroy(Service $service)
    {
        $service->delete();
        return redirect()->route('services.index')->with('success', 'Услуга удалена');
    }
}
```

---

✅ **Что мы сделали:**

* Полный CRUD для квартир, поставщиков и услуг.
* Валидация данных.
* Редирект с flash-сообщением после действий.
* Совместимость с нашими Blade-шаблонами.

---

Если хочешь, следующим шагом я могу сделать **контроллеры для тарифов (Rates), начислений (Charges) и платежей (Payments) с расчетом баланса квартиры**, чтобы веб-приложение стало полностью рабочим для учета квартир.

Хочешь, чтобы я это сделал?

---

[Предыдущая часть](05.md) | [Содержание](01.md) | [Следующая часть](07.md)


