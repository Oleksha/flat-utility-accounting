## Загрузка и хранение квитанции по начислениям

Продолжим проект

Отлично 👍 продолжаем.

Сейчас проект уже **на очень хорошем уровне**:
есть квартиры, услуги, начисления, платежи, массовый ввод, отчёты, дашборд.
Самый логичный и ценный следующий шаг — **сделать систему “умной”, а не просто учётной**.

Я предложу **3 действительно правильных направления**, а ты выберешь номер.

---

## 🔥 Куда двигаться дальше

### **1️⃣ Автозачёт платежей по долгам (умный баланс) — РЕКОМЕНДУЮ**

Сейчас:

* платежи просто суммируются

Сделаем:

* платеж автоматически:

    * закрывает старые долги
    * сначала по услугам
    * потом по месяцам
* появляется **реальный остаток долга**
* можно видеть:

    * «оплачено за январь»
    * «переплата за свет»

👉 Это превращает проект в **настоящую бухгалтерию ЖКХ**

---

### **2️⃣ Тарифы по услугам + автоподстановка сумм**

Сейчас:

* суммы вводятся вручную

Сделаем:

* у услуги есть тариф:

    * фиксированный
    * за м²
    * за счётчик (позже)
* при начислении:

    * сумма подставляется автоматически
* можно менять тарифы по датам

👉 Меньше ручной работы, меньше ошибок

---

### **3️⃣ Excel / CSV (импорт и экспорт)**

Сделаем:

* экспорт:

    * все начисления за год
    * все платежи
* импорт:

    * банковская выписка
    * автоматическое создание платежей

👉 Очень полезно для реального использования

---

## 🧭 Моя рекомендация по порядку

Если делать **как в реальном продукте**:

1️⃣ автозачёт платежей
2️⃣ тарифы
3️⃣ импорт/экспорт
4️⃣ уведомления и напоминания

---

Напиши просто номер:
**1**, **2** или **3** — и я сразу начну реализацию с кодом и UX 👌

Давай сделаем хранение файлов PDF квитанций по начислениям

Отличная идея 👍
**Хранение PDF-квитанций по начислениям** — это прям **enterprise-уровень** и очень логично для ЖКХ-приложения.

Сделаем **просто, надёжно и расширяемо**, чтобы потом можно было:

* хранить несколько файлов
* заменять квитанции
* скачивать и просматривать

---

# 🎯 Как это будет работать (UX)

### 📄 На уровне начисления (Charge):

* можно **прикрепить PDF-квитанцию**
* видно:

    * 📎 есть файл / нет файла
* кнопки:

    * 👁 Открыть
    * ⬇ Скачать
    * 🗑 Удалить / заменить

---

# 🧩 Архитектура решения

### 📌 Выбор: **файл хранится в storage**

* `storage/app/public/receipts`
* в БД хранится **путь к файлу**

✔ безопасно
✔ быстро
✔ стандарт Laravel

---

# ✅ ШАГ 1. Добавляем поле в БД

### Миграция

```bash
php artisan make:migration add_receipt_to_charges_table
```

```php
public function up()
{
    Schema::table('charges', function (Blueprint $table) {
        $table->string('receipt_path')->nullable()->after('amount');
    });
}

public function down()
{
    Schema::table('charges', function (Blueprint $table) {
        $table->dropColumn('receipt_path');
    });
}
```

```bash
php artisan migrate
```

---

# ✅ ШАГ 2. Модель Charge

```php
protected $fillable = [
    'apartment_id',
    'service_id',
    'amount',
    'period',
    'receipt_path',
];
```

---

# ✅ ШАГ 3. Настройка storage (если ещё не делал)

```bash
php artisan storage:link
```

Файлы будут доступны по URL:

```
/storage/receipts/...
```

---

# ✅ ШАГ 4. Поле загрузки в форме начислений

### `charges/_form.blade.php`

Добавь в форму:

```blade
<div class="mb-3">
    <label class="form-label">PDF-квитанция</label>

    <input type="file"
           name="receipt"
           class="form-control"
           accept="application/pdf">

    @if($charge->receipt_path)
        <div class="mt-2">
            <a href="{{ Storage::url($charge->receipt_path) }}"
               target="_blank"
               class="btn btn-sm btn-outline-secondary">
                📄 Открыть квитанцию
            </a>
        </div>
    @endif
</div>
```

⚠️ **Не забудь** в `<form>`:

```blade
<form method="POST" enctype="multipart/form-data">
```

---

# ✅ ШАГ 5. Контроллер ChargeController (store / update)

### Валидация

```php
'receipt' => 'nullable|file|mimes:pdf|max:5120', // до 5 МБ
```

---

### Сохранение файла (store)

```php
if ($request->hasFile('receipt')) {
    $data['receipt_path'] = $request->file('receipt')
        ->store('receipts', 'public');
}
```

---

### Обновление файла (update)

```php
use Illuminate\Support\Facades\Storage;

if ($request->hasFile('receipt')) {

    // удалить старый файл
    if ($charge->receipt_path) {
        Storage::disk('public')->delete($charge->receipt_path);
    }

    $data['receipt_path'] = $request->file('receipt')
        ->store('receipts', 'public');
}
```

---

# ✅ ШАГ 6. Удаление файла при удалении начисления

В `destroy()`:

```php
if ($charge->receipt_path) {
    Storage::disk('public')->delete($charge->receipt_path);
}

$charge->delete();
```

---

# ✅ ШАГ 7. Отображение в таблице начислений

Во вкладке **«Начисления»**:

```blade
<td class="text-center">
    @if($charge->receipt_path)
        <a href="{{ Storage::url($charge->receipt_path) }}"
           target="_blank"
           title="Открыть квитанцию">
            📄
        </a>
    @else
        —
    @endif
</td>
```

---

# 🎉 Что в итоге

Теперь у тебя:

✔ хранение PDF-квитанций
✔ безопасное удаление / замена
✔ быстрый просмотр
✔ логичная привязка к начислению
✔ база для расширения

---

## 🔜 Очень логичные улучшения дальше

1️⃣ **Несколько квитанций на одно начисление**
2️⃣ **PDF-квитанции по платежам**
3️⃣ **Загрузка ZIP (разобрать по месяцам)**
4️⃣ **Автоприкрепление квитанций к начислениям**

Хочешь — идём дальше.
Напиши номер или идею 👌

---

[Предыдущая часть](39.md) | [Содержание](01.md) | [Следующая часть](41.md)


