## Исправление ошибок

Возникла ошибка
Error
`resources\views\apartments\show.blade.php:60`

Call to a member function format() on string

А, теперь понятно! Ошибка

```
Call to a member function format() on string
```

возникает, потому что ты пытаешься использовать метод `format()` **на строке**, а не на объекте **Carbon**. В Laravel, когда ты берёшь поле даты из базы, оно может приходить как строка, если не указано в модели как `$dates` или `$casts`.

В нашем случае это относится к **полям `period` в Charge`и`payment_date` в Payment`**.

---

# **Исправление**

## 1. В модели `Charge`:

```php
// App\Models\Charge.php
protected $dates = ['period']; // старый способ
// Или современный вариант
protected $casts = [
    'period' => 'date',
];
```

## 2. В модели `Payment`:

```php
// App\Models\Payment.php
protected $casts = [
    'payment_date' => 'date',
];
```

---

После этого Laravel автоматически конвертирует эти поля в **Carbon объекты**, и метод `format()` будет работать.

---

# **Дополнительно**

Если используешь поле `start_date` в `Rate`, тоже добавь:

```php
protected $casts = [
    'start_date' => 'date',
];
```

---

💡 **Совет:** всегда используйте `$casts` для всех полей даты, которые планируешь форматировать, чтобы Blade не ломался.

После этого ошибка на странице квартиры **исчезнет**, и даты будут правильно отображаться.

---

Если хочешь, я могу сразу прислать **обновлённые модели с правильными $casts для всех дат**, чтобы больше таких ошибок не возникало.

Хочешь, чтобы я это сделал?

---

[Предыдущая часть](14.md) | [Содержание](01.md) | [Следующая часть](16.md)


