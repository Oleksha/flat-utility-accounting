## Ошибка при добавлении квартиры

Ошибка при добавлении квартиры Add [_token] to fillable property to allow mass assignment on [App\Models\Apartment].

А, теперь понятно! Эта ошибка связана с **Mass Assignment в Laravel**. Когда ты используешь `$request->all()` в `Apartment::create($request->all())`, Laravel проверяет, какие поля разрешено массово заполнять через **fillable**. Если поля не указаны — возникает ошибка про `_token`.

Исправляется просто: нужно добавить массив `$fillable` в модель `Apartment`.

---

## **Исправление для Apartment**

```php
<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;

class Apartment extends Model
{
    use HasFactory;

    // Разрешаем массовое заполнение этих полей
    protected $fillable = [
        'name',
        'address',
    ];

    // Отношения
    public function charges()
    {
        return $this->hasMany(Charge::class);
    }

    public function payments()
    {
        return $this->hasMany(Payment::class);
    }
}
```

---

### 🔹 Важное

То же самое нужно сделать для **Provider** и **Service**, если используешь `$request->all()`:

#### Provider

```php
protected $fillable = ['name', 'phone', 'email', 'site_url'];
```

#### Service

```php
protected $fillable = ['name'];
```

---

После этого **ошибка с `_token` исчезнет** и добавление через формы будет работать корректно.

Если хочешь, я могу **сделать сразу все модели с fillable и нужными отношениями**, чтобы CRUD полностью заработал без ошибок.

Хочешь, чтобы я это сделал?

---

[Предыдущая часть](07.md) | [Содержание](01.md) | [Следующая часть](09.md)


